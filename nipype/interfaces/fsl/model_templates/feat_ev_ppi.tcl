# Interactions (EV $ev_num with EV $ev_tmp)
set fmri(interactions$ev_num.$ev_tmp) $ev_bool

# Demean before using in interactions (EV $ev_num with EV $ev_tmp)
set fmri(interactionsd$ev_num.$ev_tmp) $ev_flt

